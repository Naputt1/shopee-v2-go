# AGENT — Generated code: DO NOT EDIT

> This package is generated by [`@doclient`](https://github.com/Naputt1/doclient) (`doclient.config.ts` + `profile.ts` + `source-shopee.ts`).

**Never edit (overwritten by `pnpm run generate` / `.github/workflows/weekly-generate.yml`):**
- `*.gen.go`, `*.type.gen.go`, `errors.gen.go`, `*_test.go` (generated), `fixtures/*.json`, `.cache/`
- Scaffolds `goshopee.go` + `auth.go` — rendered from `templates/client.go` + `templates/auth.go`

**Edit instead:**
- Client logic / query serialization (e.g. `category_id_list`, `item_id_list`) → `templates/client.go`
- Types, enums, overrides → `profile.ts` / `doclient.config.ts:mappings`
- API discovery / params → `source-shopee.ts`

**After template/profile change:** `pnpm install --frozen-lockfile && pnpm run generate` then `go vet ./... && go test ./...` must pass. Commit template **and** generated output together — weekly `git add -A` will revert a hotfix that touches only `goshopee.go`.
